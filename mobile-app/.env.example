# Safe default for local setup: do not point to production from examples.
# Copy the right profile file and override this value in your local `.env`.
EXPO_PUBLIC_API_BASE_URL=https://staging.example.com
